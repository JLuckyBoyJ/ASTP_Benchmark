- Try combining various factors to determine how promising it is to select an arc.
- Try sparsifying the matrix by setting unpromising elements to zero.
- The matrix is asymmetric: the reverse arc distance_matrix[j][i] is extra information, not a duplicate of distance_matrix[i][j].
