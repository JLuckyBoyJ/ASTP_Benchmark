"""The meta level: optimizers, not heuristics.

`seed_algorithm.py`          I_0, the baseline optimizer a run is judged against
`seed_algorithm_improved.py` the function that performs the first outer-loop step
`paper_optimizer.py`         an optimizer MoH discovered on TSP, as an alternative seed
"""
