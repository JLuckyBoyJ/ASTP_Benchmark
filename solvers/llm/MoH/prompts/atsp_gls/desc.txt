Task: Given the cost matrix of an Asymmetric Travelling Salesman Problem (ATSP), the tour the local search is currently stuck in, and how many times each directed arc has already been penalised, please help me design a strategy to update the arc cost matrix so the search escapes the local optimum, with the final goal of finding a closed tour of minimum total cost.
First, describe your new algorithm and main steps in one sentence. The description must be inside a brace and marked as a comment. Next, implement it in Python as a function named update_edge_distance. This function should accept 3 input(s): 'edge_distance', 'local_opt_tour', 'edge_n_used'. The function should return 1 output(s): 'updated_edge_distance'.
'edge_distance' is the (n, n) ATSP cost matrix, where entry [i, j] is the cost of travelling FROM city i TO city j. It is asymmetric: edge_distance[i, j] != edge_distance[j, i] in general, and the diagonal is 0. 'local_opt_tour' is a 1-D integer array of length n giving the current local optimal tour as 0-indexed city IDs (0 to n-1, indexed with 1 index like local_opt_tour[i], NEVER 2D like local_opt_tour[u, v]). NEVER subtract 1 from city IDs (use u = local_opt_tour[i], NOT local_opt_tour[i] - 1). The tour uses the directed arcs (local_opt_tour[i] -> local_opt_tour[i+1]) and finally (local_opt_tour[-1] -> local_opt_tour[0]). 'edge_n_used' is the (n, n) matrix of how many times each DIRECTED arc has been penalised so far. All are Numpy arrays. 'updated_edge_distance' must be an (n, n) Numpy array of finite values with the same shape as 'edge_distance'.
The returned matrix is used only to RANK the arcs of the current tour: the search abandons the tour arc with the largest updated_edge_distance[u, v] / (1 + edge_n_used[u, v]). It is never added to the tour cost, so only relative values matter, and returning the input unchanged reduces the method to classic guided local search. To penalise arcs you want the search to abandon, increase updated_edge_distance[u, v] on those arcs. Avoid direct division by edge_n_used without adding 1 (e.g. use 1.0 / (1.0 + edge_n_used) instead of 1.0 / edge_n_used) to prevent division by zero for unvisited edges. Always use modulo `(i + 1) % n` or check bounds when indexing `local_opt_tour[i]` to prevent `IndexError: index out of bounds`. Do NOT add `edge_n_used` directly to `updated_edge_distance` without inverse weighting, as increasing `updated_edge_distance` on heavily penalised arcs cancels out the denominator penalty. NEVER multiply non-tour arcs or the cost matrix by decay factors < 1.0 (e.g. matrix * 0.9 or matrix[not_tour] *= 0.5) as repeated multiplicative decay collapses the cost matrix to zero, destroying distance ratios and causing local search to fail. Use np.clip or bounded operations for exponential multipliers (e.g. np.clip(np.exp(...), 0, 1e6)) to guarantee finite values without inf or nan. Never write `if array_slice:` (e.g. `if edge_n_used[u, v] > 5:`) because evaluating multi-element arrays in Python `if` raises ValueError — use `np.where(condition, value_if_true, value_if_false)` or scalar loops `for i in range(n):` instead.
CRITICAL SIGNATURE & VARIABLE RULES:
1. The function signature MUST accept EXACTLY 3 inputs: `def update_edge_distance(edge_distance, local_opt_tour, edge_n_used)`. NEVER add extra parameters like 'iteration' or 'step'.
2. Do NOT use undefined loop variables like 'i' in vectorized code. To get both endpoints of all tour arcs vectorized, use: `u = local_opt_tour; v = np.roll(local_opt_tour, -1)`. NEVER write nested `for i in range(n): for j in range(n):` loops — matrix updates MUST be vectorized or 1D over tour arcs `[u, v]` to avoid $O(n^2)$ Python loop timeouts on large instances $n \ge 300$.
3. Check boolean array masks before applying operations or indexing (e.g. `arr[mask][-1] if np.any(mask) else default_val`) to avoid zero-size array reduction, empty `(0,)` shape broadcast errors, and `IndexError: index is out of bounds for size 0`.
4. Always wrap list indices and range bounds in `int()`, e.g. `my_list[int(idx)]` and `range(int(0.5 * n))` to prevent `TypeError: list indices must be integers or slices, not numpy.float64`. Use `sum()` or `np.sum(list)` instead of `np.sum(generator)`. Ensure matrix updates maintain exact `(n, n)` 2D shape (use `updated_edge_distance[u, v]` for 1D tour arc updates). Do NOT apply 2D boolean masks of shape `(n, n)` onto 1D tour arrays of shape `(n,)`. Do NOT use `u[:, None]` or `u[:, np.newaxis]` when indexing 2D cost matrices, as inserting new axes creates 3D tensors causing `IndexError: index is out of bounds for axis 1 with size 1`.
Do not change the function name. Remember to use # at the beginning of each comment inside and outside the codeblock. Ensure that the function begins by creating a copy of the cost matrix to avoid modifying the original input directly. Never write to edge_distance, local_opt_tour or edge_n_used in place. Treat the matrix as directed: an update applied to [u, v] should not be mirrored onto [v, u] unless your idea specifically calls for it. Write Python code that is deterministic and reproducible. NEVER include any random or non-deterministic snippets in the code. For example: import random, np.random, random.randint, etc. We will not take solutions with random into consideration. The function is called once per outer iteration of the search, so keep it vectorised with numpy; an O(n^3) Python loop will exhaust the evaluation budget before the search makes progress. Do not give additional explanations.
