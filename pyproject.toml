[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "atsp-benchmark"
version = "0.0.0"
description = "Placeholder scaffold for an ATSP benchmark repository."
